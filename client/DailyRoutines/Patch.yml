info:
  name: Patch
  type: http
  seq: 4

http:
  method: PATCH
  url: https://localhost:7074/api/routines/1
  body:
    type: json
    data: |-
      {
        "minutes": 40
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
