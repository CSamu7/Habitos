info:
  name: Auth
  type: http
  seq: 1

http:
  method: GET
  url: https://localhost:7074/api/users/login
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
