info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: https://localhost:7074/api/users/login
  body:
    type: json
    data: |-
      {
        "username": "sam",
        "password": "7263bmaj$_"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
