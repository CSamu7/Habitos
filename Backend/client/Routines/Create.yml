info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: https://localhost:7074/api/users/samu/routines/
  body:
    type: json
    data: |-
      {
        "name": "zzz",
        "minutes": 30,
        "idCategory": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
