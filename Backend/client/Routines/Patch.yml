info:
  name: Patch
  type: http
  seq: 3

http:
  method: PATCH
  url: https://localhost:7074/api/routines/1
  headers:
    - name: Content-Type
      value: application/json-patch+json
  body:
    type: json
    data: |-
      [
        {
          "op": "replace",
          "path": "fafa",
          "value": "fff"
        }
      ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
